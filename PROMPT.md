# PROMPT.md — وب‌سایت سینا گل‌نقره (بیسیست)
*(سوپرپرامپت تجربه — هر مدلی با همین فایل می‌تونه سایت رو بازسازی یا ری‌تم کنه)*

## تجربه در یک جمله
ورود به دنیای سازِ سینا: لندینگِ نام، بعد سردسته‌ی Dingwall NG2 که گوشی‌هاش با اسکرول می‌چرخن، و بعد مسیر بیو → موزیک → کنسرت‌ها → تماس.

## ساختار قفل‌شده
1. **Hero** — نام هنری + tagline
2. **سردسته NG2** 🔥 — لحظه‌ی امضا (مشخصات پایین)
3. **بیو** — متن واقعی از Bio.txt
4. **موزیک** — پلیر نمونه صدا (placeholder آماده)
5. **کنسرت‌ها** — لیست تاریخ‌محور
6. **تماس** — شبکه‌ها

## لحظه‌ی امضا: سردسته NG2 (سکشن ۲)
- سردسته‌ی گیتار از **راست کادر زده بیرون**؛ ثابت می‌مونه (pinned)، اسکرول فقط گوشی‌های کوك رو می‌چرخونه.
- هر گوشی در نقطه‌ای از اسکرول با easing یک «تیك» رچت می‌چرخه (نه چرخش پیوسته).
- **صدای كلیك رچت ملایم** موقع چرخش هر گوشی — WebAudio سنتز شده (بدون فایل خارجی)، پس از اولین تعامل کاربر.
- روی تصویر فقط یه لیبل کوچک «Dingwall NG2» — بدون هیچ کپی دیگه.
- پایان سکشن: سردسته آروم از کادر خارج میشه → اسکرول عادی به بیو (بدون کات).
- **منبع بصری**: کلیپ/عکس واقعی از گیتار خود سینا (در حال تهیه). ترجیح راه A: کلیپ موبایل کلوزآپ گوشی‌ها ← تبدیل به فریم‌های وب‌پی + post-process نور/grain در WebGL. fallback: عکس‌های چندزاویه → پرداخت WebGL یا مدل سه‌بعدی. آخرین گزینه: AI video (گوگل فلو) — ریسک ذوب شدن جزئیات مکانیکی دارد.
- کیفیت بار بالا: grain فیلمی، vignette، هایلایت فلزی سر پیچ، پارالاکس موس ملایم.

## محتوای واقعی (harvest شده از content/)
- نام: **سینا گل‌نقره** — نوازنده گیتار بیس، آهنگساز، مهندس صدای زنده.
- شروع: ۱۳۸۳ | مسترکلاس بابک ریاحی‌پور | مهندسی صدا زیر نظر روزبه اسماعیلی و سپهر حقیقی (۲۰۱۵).
- گروه‌ها: **ماخولا** (راک فارسی، تور رشت/شیراز/تهران-برج آزادی)، **کارنیوالسک** (۸ آلبوم تجربی/پراگرسیو)، مونهد (آلبوم افق رویداد)، مگو، جلیل. سرپرست موسیقی پینوکیوی موزیکال ۲۰۱۹.
- دبی از ۲۰۲۳: اجرا با سیاوش شمس، فتانه، کوروس.
- مهندسی صدا: صدابردار ثابت بالزن، ماخولا، نیواگ، مجتبی اخوان.
- لینک‌ها: instagram.com/sina.golnoghreh · instagram.com/makhoola.official · instagram.com/carnivalesqueband · carnivalesqueband.bandcamp.com
- ساز: **Dingwall NG2**.
- تصاویر: ۱۴ عکس اجرای زنده در content/.

## معماری
- تک‌فایل `index.html` + `assets/three.min.js` (vendored) + `assets/fonts/*.ttf` (وزیرمتن) — کاملاً آفلاین، `file://`.
- دوزبانه: فارسی RTL پیش‌فرض، toggle انگلیسی (`dir` swap).
- canvas ثابت full-viewport زیر editorial HTML.
- state hook تست: `window.__sina.getState()` → `{p, chapter, pegIndex}`.
- reduced-motion: بدون sway/pulse؛ متن کامل خوانا. موبایل <768px: ذرات نصف.

## هویت بصری
- مشکی OLED `#000`–`#0A0A0B`، طلایی اسپات `#D9A441`، قرمز LIVE `#E5484D` (فقط دو رنگ روی سیاه).
- تایپ وزیرمتن؛ headings بزرگ editorial؛ گرین + vignette آنالوگ، نه گلو نئونی.

## قواعد کیفیت
- کنتراست ≥4.5:1، focus visible، SVG icon نه emoji، صفر درخواست شبکه در runtime.
- verify: CDP sweep — فراکشن‌ها، خطای کنسول صفر، وضعیت چرخش گوشی‌ها در اسکرول، pixel stats.
